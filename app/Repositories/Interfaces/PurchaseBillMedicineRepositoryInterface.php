<?php

namespace App\Repositories\Interfaces;

interface PurchaseBillMedicineRepositoryInterface
{
    // At this stage, the core logic is handled within the PurchaseBillRepository transaction.
    // We can add methods here later if we need more specific, granular operations
    // for individual medicines within a purchase bill.
    // For now, an empty interface is sufficient.
}